# Docs build dependencies (GitHub Pages via Actions)
#
# Keep this minimal: only what `source/conf.py` imports/enables.

sphinx>=7.2,<8
furo>=2024.1.29
myst-parser>=2.0,<3
sphinx-autodoc-typehints>=2.0,<3
